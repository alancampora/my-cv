import React from 'react';

const Left = ({photo}) => (
    <div class="Left">
        <div className="Left-photoContainer">
            <img src={{photo}} />
        </div>
    </div>
);

export default Left;
